import preferences from '../preferences';

const CAPTURE_LIMIT = 893; // Excluding pokemons 100xx (No images available)

export const getCaptures = async (query = { userId: null }) => {
  // TODO: implement user capture info
  return null;
};

export const updateCaptures = async (query = { id: null, ids: [], userId: null }) => {
  // TODO: implement user capture update
};

export const deleteCaptures = async (query = { id: null, ids: [], userId: null }) => {
  // TODO: implement user capture delete
};
